@using Nhom11_QLHocThem.Areas.Admin.Model.DTO
@model List<ThongKeLuong_GiaoVienView>
@{
    ViewBag.Title = "ThongKeLuong";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2>Thống kê khoản lương giáo viên</h2>
<div class="container">
    <div class="row">
        <div class="left col">
            @*<div class="title">
                    <h2>Thống kê các khoản lương giáo viên</h2>
                </div>*@
            <form method="post" action="/Admin/ThongKeLuongGiaoVien/ThongKeLuong">
                <div class="row">
                    <div class="form-group col">
                        <label for="ngaybatdau">Ngày bắt đầu:</label>
                        <input type="date" name="ngaybatdau" class="form-control" id="ngaybatdau">
                    </div>
                    <div class="form-group col">
                        <label for="ngayketthuc">Ngày kết thúc:</label>
                        <input type="date" name="ngayketthuc" class="form-control" id="ngayketthuc">
                    </div>
                </div>

                <div class="add mt-3">
                    <button type="submit" class="btn btn-primary mb-2">Thống kê</button>
                </div>
            </form>
        </div>
        <div class="right col">

        </div>
    </div>
    <h6 class="mt-3">Thông tin</h6>
    <div class="">
        <table class="table table-striped text-center">
            <thead>
                <tr>
                    <th scope="col">STT</th>
                    <th scope="col">Họ tên</th>
                    <th scope="col">Mức thanh toán</th>
                    <th scope="col">Tổng số lớp</th>
                    <th scope="col">Tổng số buổi</th>
                    <th scope="col">Thành tiền</th>
                    <!-- <th scope="col"></th> -->
                </tr>
            </thead>
            <tbody>
                @{int i = 0; }
                @foreach (var item in Model)
                {
                    i++;
                    <tr>
                        <th scope="row">@i</th>
                        <td>@item.TenGiaoVien</td>
                        <td>@item.TyLePhanTram</td>
                        <td>@item.TongSoLop</td>
                        <td>@item.TongSoBuoi</td>
                        <td>@item.thanhtien</td>
                        <!-- <td class="text-info">Chi tiết</td> -->
                    </tr>
                }

            </tbody>
        </table>
        <h5 class="mt-4">Tổng tiền lương:</h5>
    </div>
</div>


